export enum WebError {
  NetworkFailure = "network failure",
  ResponseNotOk = "response status code is not ok",
  ResponseNotJson = "response is not json type",
  ResponseJsonNotArticlesInfo = "response json is not article info"
}
